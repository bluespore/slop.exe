# Copy to .env.local for local overrides, or set in your Vercel project.
# Host of the game server (no protocol).
# Local dev (wrangler dev):      127.0.0.1:1999
# Production:                    slop-exe.<your-subdomain>.workers.dev
NEXT_PUBLIC_PARTYKIT_HOST=127.0.0.1:1999
